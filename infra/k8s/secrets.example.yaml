apiVersion: v1
kind: Secret
metadata:
  name: fraud-detector-secrets
  namespace: fraud-detector
  labels:
    app.kubernetes.io/name: fraud-detector
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database credentials
  POSTGRES_PASSWORD: "postgres"  # CHANGE IN PRODUCTION!
  
  # JWT and API secrets (base64 will be auto-applied)
  # Generate new values: -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})
  JWT_SECRET: "qJiktPsrzSZ2p8XMQ0aEhnKvUWDY6A5F"
  ADMIN_SECRET: "lEjTXPtzfq3Zpme2FHIYRy0h8cwir1AV"
  API_TOKEN: "vUfIt9RDa2BLiYEcmwSp5VoCkNlQMgbT"
  
  # Full database URL (constructed)
  DATABASE_URL: "postgresql+psycopg2://postgres:postgres@postgres:5432/frauddb"

---
# Docker registry secret (if using private registry)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: registry-credentials
#   namespace: fraud-detector
# type: kubernetes.io/dockerconfigjson
# data:
#   .dockerconfigjson: <base64-encoded-docker-config>
