# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env and replace all CHANGE_ME values
# 
# IMPORTANT: Never commit .env with real secrets to version control!
# ========================================

# ========== POSTGRESQL DATABASE ==========
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Mlb27ewS5DLqlQJ2
POSTGRES_PORT=5432

# Database connection string
# Local PostgreSQL:
# DATABASE_URL=postgresql+psycopg2://postgres:CHANGE_ME_STRONG_DB_PASSWORD@localhost:5432/fraud_detector_db

# For cloud/managed PostgreSQL (AWS RDS, Azure, etc.):
postgresql://postgres+psycopg2:Mlb27ewS5DLqlQJ2@db.jrjxaiatghxllukpckbb.supabase.co:5432/postgres

# ========== BACKEND API ==========
API_PORT=8000

# ========== SECURITY SECRETS ==========
# ⚠️ CRITICAL: Generate unique 32+ character secrets for production!
# Use the commands at the bottom of this file to generate secure values.
JWT_SECRET=9EGnxwZzpAdhWrsBlqIgTcLbHC0UkSNJ
ADMIN_SECRET=3HRUCW7dXmhNkGuOTVxfstq5ea2I19EZ
API_TOKEN=qx72jF3LE9eHGA08uYh5gsVMflCDSZab

# ========== FLASK ==========
FLASK_ENV=production
FLASK_DEBUG=False

# ========== FRONTEND ==========
# Set this to your actual production API URL
VITE_API_URL=https://api.yourdomain.com

# Alternative configurations:
# - Same domain with reverse proxy: VITE_API_URL=/api
# - Local development: VITE_API_URL=http://localhost:8000

# ========== MODEL THRESHOLDS ==========
# Fraud detection sensitivity (0.0 - 1.0)
# Higher = more strict, fewer false negatives but more false positives
REVIEW_THR=0.65
TX_THR=0.50

# ========== LOGGING ==========
# Options: debug, info, warning, error
LOG_LEVEL=info

# ========== GUNICORN (Production Server) ==========
# Recommended: 2-4 workers per CPU core
GUNICORN_WORKERS=4

# ========== REDIS (OPTIONAL) ==========
# Only needed if using Redis for caching/sessions
REDIS_PASSWORD=

# ========================================
# 🔐 HOW TO GENERATE SECURE SECRETS
# ========================================
#
# PowerShell (Windows):
#   -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})
#
# Bash (Linux/Mac):
#   openssl rand -base64 32
#
# Python:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# ========================================
# 📋 DEPLOYMENT CHECKLIST
# ========================================
# [ ] Replace all CHANGE_ME values with secure secrets
# [ ] Update DATABASE_URL with production database credentials
# [ ] Set VITE_API_URL to your production API endpoint
# [ ] Verify FLASK_ENV=production and FLASK_DEBUG=False
# [ ] Consider adjusting GUNICORN_WORKERS based on server specs
# [ ] Test the application locally before deploying
# ========================================
